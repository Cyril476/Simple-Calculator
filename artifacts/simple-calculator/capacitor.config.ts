import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.precision.calculator',
  appName: 'Precision Calculator',
  webDir: 'dist/public',
  androidScheme: 'https',
  loggingBehavior: 'none',
};

export default config;
